// Connecting to dbs
client = MongoClient()
client = MongoClient('localhost', 27017)

// Choose a dbs
db = client.test_collection

// Choose a table
collection = db.test_collection


post = {"author": "John",
    "text": "My second blog post!",
    "tags": ["mongodb", "python", "pymongo"],
    "date": datetime.datetime.utcnow()}



// POST one
// return format
//  + boo   acknowledged
//  + str   inserted_id
table = db.table
post = table.insert_one(post)

// POST many
// return format
//  +boo    acknowledged
//  +list   inserted_ids
table = db.table
post = table.insert_many(post)



// GET all entries
params = {
    '_id': djsakjdk
}
res = table.find(params)

// GET one entry
res = table.find_one(params)



// PUT
// return format
//  + boo   acknowledged
//  + num   matched_count
//  + num   modified_count
//  + json  raw_result
//  + num   upserted_id
update_one(), update_many(), and replace_one().